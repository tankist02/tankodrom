{
    "configurations": [
    {
        "type": "lldb",
        "request": "launch",
        "name": "Launch",
        "program": "${workspaceFolder}/target/debug/fizzbuzz",
        "args": [],
        "cwd": "${workspaceFolder}"
    }
    ]
}